# Phase 2 / Task A Report — @fabriq/ui

## STATUS: DONE
Commit: `756d0e2`

## Components created (packages/ui/src/components/ui/)
- button.tsx — cva buttonVariants + Radix Slot asChild
- card.tsx — Card/Header/Title/Description/Content/Footer
- input.tsx
- table.tsx — Table/Header/Body/Footer/Row/Head/Cell/Caption
- badge.tsx — cva badgeVariants
- skeleton.tsx
- separator.tsx — Radix SeparatorPrimitive
- scroll-area.tsx — Radix ScrollAreaPrimitive + ScrollBar
- tooltip.tsx — Radix TooltipPrimitive (Provider/Root/Trigger/Content)
- dropdown-menu.tsx — Radix DropdownMenuPrimitive (full set)
- alert.tsx — cva alertVariants + AlertTitle/AlertDescription

## CSS isolation approach (src/styles.css)
- No `@import "tailwindcss"` (would pull global preflight)
- Selective imports: `tailwindcss/theme.css` + `tailwindcss/utilities.css` only
- Light tokens on `.fabriq-admin {}`, dark on `.fabriq-admin[data-fabriq-theme="dark"] {}`
- `@custom-variant dark` keyed exclusively on `.fabriq-admin[data-fabriq-theme="dark"]`
- `@theme inline` maps all 18 CSS vars + 3 radius vars to Tailwind utilities
- `@layer base` reset uses `.fabriq-admin *` selectors only — NO bare `*`, `html`, `body`, `:root`
- Host-leak audit: grep for `^html|^body|^:root|\*\s*{` → 0 matches (confirmed)

## Version substitutions
None. All requested versions resolved from npm without substitution.
Radix packages are React 19 peer-compatible (no blocking peer warnings).

## Test results
**Red phase** (before implementation): suite-level import error — FAIL as expected.
**Green phase** (after implementation):
- `pnpm --filter @fabriq/ui test` → 8/8 pass
- Full workspace `pnpm test` → 102/102 pass across 13 test files (+8 vs prior 94)

## Build
`pnpm --filter @fabriq/ui build` (tsc) → clean, 0 errors, 0 warnings.
(styles.css is not processed by tsc — correct; CSS validated at host build time in Task B.)

## Full suite count
102 tests / 13 test files — all green.
